# 🎯 HP-ML 交互式 CLI - 快速开始

欢迎使用 HP-ML 交互式 CLI！这是一个可视化菜单系统，让您轻松完成 ETF 量化模型的训练、导出、回测和对比。

## 🚀 立即开始

```bash
# 启动交互式 CLI
make cli
```

就这么简单！CLI 会引导您完成所有操作。

## 💡 第一次使用？

跟着这个 3 步走：

### 第 1 步：选择模型
```
请选择模型类型：
❯ Ridge 岭回归（快速，推荐）  👈 推荐新手选这个
```

### 第 2 步：选择操作
```
请选择操作类型：
❯ 🎯 训练模型  👈 先训练一个模型
```

### 第 3 步：配置参数
```
历史数据起始日期 (YYYYMMDD)： [直接按 Enter]
预测未来 N 个交易日收益： [直接按 Enter]
每个指数族保留前 N 只 ETF： [直接按 Enter]
测试集交易日数： [直接按 Enter]
数据源：
❯ 东方财富 (默认)  [直接按 Enter]
```

全部使用默认值，一路按 Enter 即可！

## ⏱️ 需要多长时间？

- **首次训练**：5-10 分钟（需要下载数据）
- **再次训练**：2-3 分钟（使用缓存）
- **导出公式**：1 分钟
- **回测评估**：1 分钟

## 🎨 界面预览

```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║        HP-ML 交互式 CLI - ETF 量化训练工具                ║
║                                                           ║
║  支持模型：Ridge, HGB, RF, LSTM, Transformer 等 13 种     ║
║  功能：训练 | 导出通达信 | 回测 | 对比                    ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

请选择模型类型：
❯ Ridge 岭回归（快速，推荐） - 线性模型，训练快速
  HGB 梯度提升树 - 集成学习，性能优秀
  随机森林 - 集成学习，稳定性好
  ...
```

## 📊 能做什么？

### 🎯 训练模型
自动完成：发现 ETF → 拉取数据 → 构建特征 → 训练模型 → 生成报告

### 📤 导出到通达信
将模型导出为通达信公式，可直接在通达信软件中使用

### 📊 回测评估
计算收益率、夏普比率、最大回撤等性能指标

### ⚖️ 模型对比
自动对比多个模型的性能，生成排名报告

### 🚀 完整流程
训练 → 回测 → 导出 → 对比 一条龙完成

## 📝 常见场景

### 场景 1：我想快速训练一个模型
```bash
make cli
# 选择：Ridge → 训练 → 全部按 Enter
```
⏱️ 约 5 分钟

### 场景 2：我想导出到通达信
```bash
make cli  
# 选择：Ridge → 导出 → 选择模型 → 所有指数族
```
⏱️ 约 1 分钟

### 场景 3：我想对比不同模型
```bash
make cli
# 选择：多模型对比 → 训练 → 选择 ridge 和 hgb
# 再次运行 CLI → 选择：模型对比
```
⏱️ 约 10-15 分钟

## 🆘 需要帮助？

### 详细文档
- [完整 CLI 使用指南](docs/CLI_GUIDE.md) - 所有功能详解
- [快速入门教程](docs/CLI_QUICKSTART.md) - 5 个实用场景
- [演示脚本](examples/demo_cli_usage.py) - 代码示例

### 命令行模式
如果不想用交互式界面，也可以直接用命令：
```bash
# 训练
python -m hp_ml.train --model ridge

# 导出
python -m hp_ml.export_tdx --all-families

# 多模型
python -m hp_ml.multi_model_train --models ridge hgb
```

### 遇到问题？
1. 运行测试：`python tests/test_cli.py`
2. 检查依赖：`make setup`
3. 查看文档：`docs/CLI_GUIDE.md`

## 🎉 开始使用

就这么简单：
```bash
make cli
```

跟着菜单提示，轻松完成所有操作！

---

**更多信息**: 查看 [README.md](README.md) 了解项目详情
