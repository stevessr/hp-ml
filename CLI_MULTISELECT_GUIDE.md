# CLI 多选功能使用指南

## 更新内容

CLI 现在支持在模型选择阶段**同时选择多个模型**进行训练，大幅提升使用灵活性！

## 功能特性

### ✨ 核心功能
- ✅ **多模型选择**: 使用复选框界面，空格键多选模型
- ✅ **批量训练**: 一次性训练多个模型
- ✅ **智能流程**: 根据选择的模型数量自动优化工作流程
- ✅ **灵活对比**: 训练后自动进行模型性能对比

### 📊 支持的工作流程

#### 1. 单模型训练（传统方式）
选择 1 个模型 → 训练 → 回测 → 导出 → 完成

#### 2. 多模型批量训练（新增）
选择多个模型 → 逐个训练 → 完成

#### 3. 多模型完整流程（新增）
选择多个模型 → 两种模式可选：
- **批量模式**: 训练所有 → 统一回测对比 → 选择最佳导出
- **逐个模式**: 每个模型独立完成训练→回测→导出流程

## 使用方法

### 启动 CLI

```bash
python -m hp_ml.cli
```

### 操作步骤

#### 第1步: 选择模型（支持多选）

```
请选择模型类型（空格多选，Enter确认）：
 ○ Ridge 岭回归（快速，推荐） - 线性模型，训练快速，支持精确导出通达信公式
 ○ HGB 梯度提升树 - 集成学习，性能优秀，支持近似导出
 ○ 随机森林 - 集成学习，稳定性好，支持近似导出
 ○ GRU 深度学习 - GRU 深度学习模型，比 LSTM 更快
 ○ 双向 GRU - 双向 GRU，捕捉前后时序依赖
 ○ 注意力 GRU - 带注意力机制的 GRU，更高效
 ○ 多头注意力 GRU - 多头注意力机制 GRU，更高效
 ○ 分层注意力 GRU - 分层注意力 GRU，捕捉多尺度模式
 ...
```

**操作提示**:
- 使用 **↑↓ 方向键** 移动光标
- 按 **空格键** 选择/取消选择模型（会显示 ✓）
- 按 **Enter** 确认选择

#### 第2步: 选择操作类型

```
请选择操作类型：
 > 🎯 训练模型 - 训练新模型或更新现有模型
   📤 导出到通达信 - 将模型导出为通达信公式文件
   📊 回测评估 - 对训练好的模型进行回测评估
   ⚖️ 模型对比 - 对比多个模型的性能指标
   🚀 完整流程 - 训练→回测→导出→对比 一条龙
```

#### 第3步: 配置训练参数

```
⚙️  配置训练参数（按 Enter 使用默认值）

历史数据起始日期 (YYYYMMDD)：20180101
预测未来 N 个交易日收益：5
每个指数族保留前 N 只 ETF：3
测试集交易日数：252
数据源：东方财富 (默认) / 通达信 (更稳定)
```

#### 第4步: 开始训练

系统会自动：
- 逐个训练选择的模型
- 显示训练进度
- 保存所有模型和结果

## 使用场景

### 场景1: 快速对比多个模型

**目标**: 比较 GRU 和 LSTM 的性能差异

**操作**:
1. 多选: `GRU 深度学习` + `LSTM 深度学习`
2. 选择操作: `训练模型`
3. 配置参数并开始训练
4. 训练完成后，选择 `模型对比` 查看差异

### 场景2: 评估所有 GRU 变种

**目标**: 找出最适合当前数据的 GRU 变种

**操作**:
1. 多选所有 GRU 变种:
   - GRU 深度学习
   - 双向 GRU
   - 注意力 GRU
   - 多头注意力 GRU
   - 分层注意力 GRU
   - Transformer GRU 混合
2. 选择操作: `完整流程`
3. 选择模式: `批量训练所有模型 → 统一回测对比 → 选择最佳模型导出`
4. 系统自动完成所有步骤

### 场景3: 传统模型 vs 深度学习

**目标**: 对比传统机器学习和深度学习的效果

**操作**:
1. 多选:
   - Ridge 岭回归（快速，推荐）
   - HGB 梯度提升树
   - 注意力 GRU
   - GRU-Transformer 混合
2. 选择操作: `完整流程`
3. 选择批量模式
4. 查看对比报告，选择最佳模型

## 示例输出

### 选择 3 个模型时

```
✓ 已选择 3 个模型：
  - GRU: GRU 深度学习模型，比 LSTM 更快
  - ATTENTION_GRU: 带注意力机制的 GRU，更高效
  - MULTIHEAD_ATTENTION_GRU: 多头注意力机制 GRU，更高效

✓ 已选择操作：train - 训练新模型或更新现有模型

⚙️  配置训练参数（按 Enter 使用默认值）
...

============================================================
训练模型 1/3: GRU
============================================================
🎯 开始训练：GRU 模型
...
✅ 训练完成！

============================================================
训练模型 2/3: ATTENTION_GRU
============================================================
🎯 开始训练：ATTENTION_GRU 模型
...
✅ 训练完成！

============================================================
训练模型 3/3: MULTIHEAD_ATTENTION_GRU
============================================================
🎯 开始训练：MULTIHEAD_ATTENTION_GRU 模型
...
✅ 训练完成！

============================================================
🎉 操作完成！感谢使用 HP-ML CLI
============================================================
```

## 完整流程的两种模式

### 模式1: 批量训练 → 统一对比（推荐）

**流程**:
```
训练模型1 → 训练模型2 → 训练模型3 →
→ 回测所有模型 → 性能对比 → 选择最佳模型导出
```

**优势**:
- 一次性完成所有训练
- 统一的回测和对比标准
- 自动选择最佳模型
- 节省配置时间

**适用**: 需要系统性对比多个模型时

### 模式2: 逐个完整流程

**流程**:
```
模型1: 训练 → 回测 → 导出
模型2: 训练 → 回测 → 导出
模型3: 训练 → 回测 → 导出
```

**优势**:
- 每个模型独立完整
- 可以针对每个模型单独配置
- 中途可以中断

**适用**: 需要对每个模型进行不同配置时

## 技术实现

### 修改的函数

#### `select_model()` - 支持多选
```python
def select_model() -> list[dict[str, Any]]:
    """选择模型类型（支持多选）"""
    # 使用 questionary.checkbox 替代 questionary.select
    answers = questionary.checkbox(
        "请选择模型类型（空格多选，Enter确认）：",
        choices=choices,
        style=custom_style,
    ).ask()
    # 返回列表而不是单个配置
    return selected_models
```

#### `main()` - 处理多模型
```python
def main():
    # 获取模型列表
    model_configs = select_model()

    # 显示选择的模型
    print(f"\n✓ 已选择 {len(model_configs)} 个模型：")

    # 根据模型数量调整流程
    if len(model_configs) > 1:
        # 多模型批量处理逻辑
        ...
    else:
        # 单模型处理逻辑
        ...
```

## 注意事项

1. **训练时间**: 选择多个深度学习模型会显著增加训练时间
2. **内存占用**: 同时训练多个模型可能需要更多内存
3. **模型保存**: 每个模型都会保存到独立的文件
4. **对比功能**: 需要至少 2 个模型才能进行对比

## 推荐组合

### 快速评估（5-10分钟）
- Ridge 岭回归
- HGB 梯度提升树
- 注意力 GRU

### 深度学习对比（30-60分钟）
- GRU 深度学习
- 双向 GRU
- 注意力 GRU
- 多头注意力 GRU

### 全面评估（1-2小时）
- Ridge 岭回归
- HGB 梯度提升树
- 注意力 GRU
- 多头注意力 GRU
- 分层注意力 GRU
- GRU-Transformer 混合

## 常见问题

### Q: 如何取消已选择的模型？
**A**: 再次按空格键即可取消选择（✓ 会消失）

### Q: 是否支持一次选择所有模型？
**A**: 可以，但建议根据实际需求选择，避免训练时间过长

### Q: 多模型训练是否可以中断？
**A**: 可以按 Ctrl+C 中断，已训练的模型会被保存

### Q: 如何查看多个模型的对比结果？
**A**: 训练完成后，选择"模型对比"操作，系统会自动读取所有模型并生成对比报告

### Q: 单选和多选的区别？
**A**: 功能完全相同，多选只是提供了更灵活的批量操作方式

## 更新日志

### 2024-06-09
- ✅ 新增多选功能
- ✅ 优化多模型训练流程
- ✅ 添加批量模式和逐个模式
- ✅ 改进用户体验和提示信息

## 反馈

如有问题或建议，请查看：
- [GRU_ATTENTION_MODELS.md](GRU_ATTENTION_MODELS.md) - 模型详细文档
- [README.md](README.md) - 项目主文档
- [test_cli_multiselect.py](test_cli_multiselect.py) - 测试脚本

---

**开始使用多选功能，提升您的模型训练效率！🚀**
